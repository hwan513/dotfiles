cacheval brew /opt/homebrew/bin/brew shellenv

# load in the brew completions, on config also set `brew completions link` to get package completion working
if type brew &>/dev/null
then
  FPATH="$(brew --prefix)/share/zsh/site-functions:${FPATH}"
  autoload -Uz compinit && compinit
fi

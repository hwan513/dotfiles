# might also need to load in the brew completions with `brew completions link`
_cacheval brew /opt/homebrew/bin/brew shellenv

# rosetta version of homebrew
alias ibrew="arch -x86_64 /usr/local/bin/brew"
alias idotnet="arch -x86_64 /usr/local/bin/dotnet"
alias ipipx="arch -x86_64 /usr/local/bin/pipx"
